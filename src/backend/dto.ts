export type Sentence = {
  title: string;
  content: string[];
  uid: string;
  sentenceId: string;
};

export type Record = {
  speed: number;
  accuracy: number;
};

export type UserInfo = {
  name: string;
  photoUrl: string;
  email: string;
};
